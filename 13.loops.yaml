- name: Install packages
  hosts: web
  become: yes
  tasks:
  - name: Installed linux packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: installed
    loop:
    - gcc
    - git
    - mysql
    - zip
